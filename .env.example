# Used by the local dev proxy/server only — never expose client_secret to the browser.
# The <lit-talk> component itself does NOT read these; it reads `github-oauth-options`.
client_id=
client_secret=
owner=
repo=
postUniqueId=
label=
